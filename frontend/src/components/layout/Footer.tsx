export default function Footer() {
  return (
    <footer className="border-t border-border mt-auto py-4 sm:py-6 text-center text-muted text-[10px] sm:text-xs px-3">
      <p>© {new Date().getFullYear()} App Promoções — Conectando você às melhores ofertas</p>
    </footer>
  )
}
